---
interface Props {
  label: string;
  title: string;
  description: string;
}
const { label, title, description } = Astro.props;
---

<article class="card">
  <span class="tag">{label}</span>
  <h3>{title}</h3>
  <p>{description}</p>
</article>
